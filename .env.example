# 공공데이터포털에서 발급된 일반 인증키
# 실제 값은 .env.local에만 저장하고 Git에 커밋하지 않습니다.
TOUR_API_KEY=

# (선택) 기상청 단기예보 API 인증키.
# 비워두면 TOUR_API_KEY 로 시도합니다 (같은 공공데이터포털 계정에서
# 기상청 단기예보 조회서비스를 활용신청한 경우 동일 키가 동작).
# 미신청·실패 시 기상은 "확인 불가"로 표기되고 판정에 반영되지 않습니다.
KMA_API_KEY=
