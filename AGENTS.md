# SafeHour 개발 에이전트 규칙

이 저장소의 변경은 Notion `기획산출물_v1.0 - SAFEHOUR`의 D01–D09를 구현 증거로 전환하는 작업이다. 원문 문서는 모두 `WORKING` 상태이므로 에이전트가 정책, 담당자, 승인 또는 KPI를 임의로 확정하지 않는다.

## 작업 시작 순서

1. `docs/AX_WORKFLOW.md`를 읽는다.
2. `docs/DEVELOPMENT_READINESS.md`에서 현재 증거와 문서 드리프트를 확인한다.
3. `docs/AX_BACKLOG.md`에서 작업 ID를 고르거나 동일 형식으로 새 ID를 만든다.
4. 변경할 Dxx 요구사항과 D09 인수조건을 작업 설명에 명시한다.
5. 코드 변경 전에 실패·경계·완료 조건을 테스트 가능한 문장으로 고정한다.

## 안전 불변조건

- 유효한 최신 병원 조건이 없거나 상충하면 추천하지 않는다.
- 증상, 시술 종류 또는 회복 단계로 의료 판단을 생성하지 않는다.
- 현재 GPS를 수집하거나 서버·제3자에게 전송하지 않는다. `USER_SELECTED_FIXED` 기준점만 허용한다.
- 병원 목록·순위·평점·제휴 추천을 만들지 않는다.
- `unknown`, `stale`, 폴백 데이터를 긍정 사실로 바꾸지 않는다.
- TourAPI 원문과 SafeHour 추정값을 같은 필드에 섞지 않는다.
- API 키, 전체 query URL, 병원 안내 원문, 이름·연락처·증상 원문을 코드·로그·문서·테스트 fixture에 남기지 않는다.
- 변화 이벤트는 안전 게이트부터 재판정하며, 필요한 변화가 화면에 반영되지 않으면 실패로 처리한다.

## 변경 단위

한 작업은 가능하면 1–2일 크기로 유지한다. 각 PR 또는 인계에는 다음을 남긴다.

- 작업 ID와 연결된 Dxx/AC/QA ID
- 사용자 가치와 범위 밖 항목
- 변경 파일과 중요한 결정
- 실행한 검증 명령과 결과
- 남은 위험, 사람 승인 항목, Notion 동기화 필요 여부

정책·API 계약·상태 의미가 바뀌면 코드만 고치지 말고 추적 문서와 관련 테스트를 함께 갱신한다. 중요한 비가역 결정은 `docs/decisions/`에 ADR로 남긴다.

## 검증 기준

- 일반 변경: `npm run lint`, `npm test`
- UI·라우팅·빌드 설정 변경: 위 검증 + `npm run build`
- 의존성 변경: 위 검증 + `npm run audit`
- 엔진·안전 게이트·SLA·이벤트 변경: 관련 단위 테스트 + 전체 `npm test`
- 외부 API 변경: fixture 테스트를 먼저 실행하고, 키가 준비된 환경에서만 실API 증거를 별도로 생성한다.

`npm run check`는 로컬·CI 공통 기준선이다. 실API 명령은 호출량과 증적을 만들기 때문에 자동 CI에서 실행하지 않는다.

## Git과 리뷰

- 기본 브랜치는 `main`이다.
- 브랜치명은 `feature/AX-###-short-name`, `fix/AX-###-short-name`, `docs/AX-###-short-name` 형식을 권장한다.
- 사용자를 위한 PR 제목과 본문은 한국어로 작성한다.
- 위험 추천, GPS, 비밀정보, 의료 판단, 저작권 위반 가능성이 있으면 병합보다 release hold를 우선한다.
