> **문서 상태: WORKING** · 시각 원본은 여전히 Figma가 소유할 예정이나, **현재 정본은 코드다.** 색·글꼴·모서리·그림자는 `app/globals.css` 의 `:root` 로 확정됐고, 여백 토큰과 Figma 파일은 아직 없다.

## 문서 메타데이터

- Document Code: D08
- Document Key 후보: safehour:D08
- Candidate Version: v1.1
- Scale Tier: Large
- Schema Version: AX-DOC v4.8.0
- 담당자 R/A: 디자인 리드 · [지정필요]
- 공동 R: 프론트엔드 개발 리드 · [지정필요]
- 상위 입력: D03·D04·D07 WORKING, `docs/PRODUCT_DEFINITION.md`, ADR-0001·0003·0004
- 목표 게이트: G2 Design Handoff Ready, G3 Dev Ready
- Figma File: [확인필요] — 원본 파일 없음. 코드가 앞서 있다
- Code package: Next.js App Router · `app/globals.css` + `components/*.js` (9개 파일)
- 개정: 2026-08-27 (병원 지침 연동 구조 전환 반영)

### 이번 개정에서 무엇이 바뀌었나

| | 이전 (2026-07-30) | 지금 |
| --- | --- | --- |
| 조건 입력 | 환자가 병원 안내문을 읽고 폼에 옮겨 적는다 | **병원이 발행하고 환자는 코드만 넣는다.** 수기 입력 화면 제거 (AX-221) | | 화면 수 | SCR001–SCR008 (8종) | **9종** — `/` `/login` `/link` `/today` `/guide` `/plan` `/result` `/place/{id}` `/privacy` | | 출처 구분 | 2영역 (TourAPI 원문 / SafeHour 추정) | **3영역** — 병원 원문 / SafeHour 판정 / 관광정보 | | 디자인 토큰 | 전부 `[Figma에서 확정]` | **색·글꼴·모서리·그림자는 코드로 확정.** 여백·모션은 미토큰화 | | 접근성 | 계약만 서술 | **실측**: axe 9화면 위반 0, 키보드 7건, 반응형 5폭 · 200% 확대 · 44px | | 언어 | ko·en 후보 | **전 화면 ko·en.** 안전 문구는 두 언어 병기 (ADR-0003) |  ---  ## 1. 컴포넌트 원칙  원칙 자체는 유지된다. 아래 두 줄이 추가됐다.  - 안전 상태와 다음 행동이 브랜드 장식보다 우선한다. - 상태는 색상만으로 구분하지 않고 텍스트·아이콘·구조를 함께 사용한다. - 의료 판단처럼 보이는 시각 표현을 금지한다. - **출처를 세 갈래로 시각 분리한다** — 병원 원문(채널 B) / SafeHour 판정·추정 / 관광정보 원문. (기존 "원천 관광정보·SafeHour 추정·병원 조건" 의 확장이다) - **판정에 쓰이는 것과 표시만 하는 것을 시각적으로 같게 보이게 하지 않는다.** 채널 B 문장은 절대 판정 근거로 읽히면 안 된다. - loading·empty·error·stale·disabled 계약을 모든 데이터 컴포넌트에 포함한다. - 모바일 360px에서 주요 행동이 가려지지 않아야 한다. - 키보드·스크린리더·44px 터치영역·WCAG AA 대비를 기본으로 한다. - **안전 문구는 두 언어를 함께 보여준다** (§5.1).  ---  ## 2. 컴포넌트 현황  기존 CMP ID 는 보존한다. 다만 **파일로 존재하는 컴포넌트는 9개**이고 나머지는 화면 안의 인라인 마크업 + CSS 클래스 규약으로 남아 있다. 이 문서는 그 사실을 숨기지 않는다.  ### 2.0 실체 구분  | 형태 | 뜻 | 개수 | | --- | --- | --- | | **파일** | `components/*.js` 로 재사용 가능하게 분리됨 | 9 |
| **규약** | 화면에 인라인으로 쓰이고 CSS 클래스만 공유 | 8 | | **폐기** | 구조 전환으로 사라짐. 번호 재사용 금지 | 2 |  ### 2.1 파일 컴포넌트 (9)  #### D08-CMP006 SafetyDecisionBanner — `components/StateBanner.js` (파일)  - 역할: 4상태와 사유 코드, 1순위 행동 표시 - 사용 화면(컴포넌트로): `/today` · `/plan`(연결 게이트 차단 시) · `/result` - **주의**: `/`(시작) `/link`(오류) `/login`(오류) `/guide`(미확인 안내) `/place/{id}`(차단·미표시) 는 컴포넌트를 쓰지 않고 `.state-banner` 클래스만 인라인으로 재사용한다 — 병기 규칙이 적용되지 않는다 - Data source: 판정 응답의 `state`, `reasons` - Variant: `NO_TOURISM` · `STANDBY` · `SPLIT_NEARBY` · `TOGETHER` - 상태: - D08-CMP006-CST001 default - D08-CMP006-CST002 updated (`live` prop → `aria-live="polite"`) - D08-CMP006-CST003 return_now - Props: `state`(required) · `reasons`(array) · `live`(boolean) - **두 언어 병기**: 현재 언어의 상태 문구를 `h2` 로, 반대 언어 문구를 `lang` 속성을 붙인 `p`(14px)로 함께 출력한다. **opacity 로 흐리게 하지 않는다** — 배너 배경 위에서 4.5:1 을 넘지 못하기 때문이다 (코드 주석에 명시) - 금지: NO_TOURISM 을 오류 빨간 화면으로만 표현. 이 배너는 정상 결과 배너다 - 클래스: `.state-banner` + `.state-{STATE}` - 정책: ADR-0003 §5, D05 7장 - 이전 대비: `returnNow`·`onPrimaryAction` prop 은 **없다.** 복귀 행동은 CMP012 가 따로 소유한다  #### D08-CMP008 RecommendationCard — `components/CourseCard.js` (파일)  - 역할: 안전 게이트를 통과한 장소와 선택 근거 표시 - 사용 화면: `/result` - 표시: 순위 배지, 장소명(원문 언어 `lang` 표기), 언어 배지, "번역 필요" 배지, 이동시간(+폴백이면 "추정" 배지), 체류(+축소 표시), 복귀 여유, 실내·실외·불명, 보행시간, 상세 CTA - Props: `candidate` · `rank` · `mode`(`patient|companion`) - 상태: default / 폴백추정 / `indoor === null` 불명 / `shrunk` - 금지: 평점·인기·제휴를 안전 근거로 표시 - 클래스: `.course-card` · `.badge-estimate` · `.badge-lang` - 이전 대비: `selected` · `replacement` · `image_error` · `disabled` 상태와 `onSelect` 는 구현되지 않았다. 카드 선택 개념이 없고 상세는 라우트(`/place/{id}`)로 간다  #### D08-CMP010 ChangeDeltaPanel — `components/DeltaSheet.js` (파일)  - 역할: 실시간 변수 이후 제거·추가·축소·상태변경 표시 - 사용 화면: `/result` 위 모달 시트 (ADR-0001 — `/result/change` 라우트는 폐기) - 표시 순서: 상태변경 → 제거(+사유) → 추가 → 축소(전→후) → 변화 없음 - Props: `recalc`(event·before·after·delta·result) · `titles` · `onClose` - 성격: **설명 전용.** 재판정 결과는 시트가 뜨기 전에 이미 적용돼 있다 (D07-BAN008) - 접근성: `role="dialog"` `aria-modal` `aria-labelledby`, 포커스 트랩, Escape·뒤로가기·확인 버튼이 모두 같은 닫기 경로(`requestClose`)를 탄다 - 클래스: `.sheet-backdrop` `.sheet` `.delta-row` `.delta-tag` - Test: E2E 겹친 시트 뒤로가기·히스토리 잉여 항목 검증 있음  #### D08-CMP012 StickyReturnAction — `.sticky-return` + `components/ReturnSheet.js` (파일)  - 역할: 결과·상세 화면에서 복귀 행동을 항상 접근 가능하게 제공 - 사용 화면: `/result`(복귀 CTA) · `/place/{id}`(결과로 돌아가기) - 시트 표시: 기준점 라벨·좌표, 복귀 마감, **좌표 복사** 버튼, 병원 연락 안내 - **지도·경로 연동 없음** — 위치정보 사전검토 회신 대기 + 공급자 미결 (D04-F012 BLOCKED). 좌표 복사와 안내 문구가 현재의 유일한 수단이다 - **두 언어 병기**: 제목·본문을 반대 언어로도 함께 출력한다 - 접근성: `env(safe-area-inset-bottom)`, `z-index: 20`(시트 50 아래·헤더 10 위 — 카드 제목의 `position: relative` 가 클릭을 가로채는 것을 막는다), 복사 결과는 `role="status"` - 이전 대비: `urgent` variant 와 `role=alert` 는 미구현. `provider_unavailable` 은 상수 상태(항상 unavailable)라 별도 variant 가 아니다  #### D08-CMP014 LanguageToggle — `components/LanguageToggle.js` (파일) · 신설  - 역할: 전 화면 상단 바에서 언어 전환 - 표시: `한국어` / `English` — **언어명은 번역하지 않는다.** 번역하면 못 찾는다 - 접근성: `role="group"` + `aria-label`, 각 버튼에 `lang` 과 `aria-pressed`, 선택 상태는 색 + 배경 반전으로 이중 표시, 최소 44px - 클래스: `.lang-toggle` - 정책: ADR-0003  #### D08-CMP015 LanguageProvider — `components/LanguageProvider.js` (파일) · 신설  - 역할: 언어 컨텍스트. 화면은 `t()` `minutesLabel()` `stateMessage()` `reasonText()` 로만 문구를 얻는다 - 결정 순서: `sessionStorage['safehour.lang']` → `navigator.language` → `ko` - 규칙: 서버 렌더는 항상 `ko`(하이드레이션 불일치 방지) · `document.documentElement.lang` 실제 갱신(WCAG 3.1.1) · **언어는 서버로 전송하지 않는다** · localStorage 신설 금지 - 정책: ADR-0003 §1–3, §7  #### D08-CMP016 ClearSessionButton — `components/ClearSessionButton.js` (파일) · 신설  - 역할: "내 정보 지우기" (AX-210) - 흐름: 1회 확인 → 세션 저장소 삭제 → `POST /api/auth/logout`(httpOnly 쿠키는 서버만 지운다) → `window.location.href = '/'` **전체 새로고침**(React 메모리의 조건 사본까지 버린다) - 접근성: 확인 문구는 `role="alert"`, 취소·삭제 버튼 44px - 클래스: `.clear-session` `.link-button`  #### D08-CMP017 FooterLinks — `components/FooterLinks.js` (파일) · 신설  - 역할: 개인정보 고지 링크 + CMP016 을 **모든 판정 화면 같은 자리**에 묶어 배치 - 사용 화면: `/` `/link` `/today` `/guide` `/plan` `/result` `/place/{id}` — **7화면**. `/login`·`/privacy` 에는 붙지 않는다 - 근거: 찾기 어려운 데이터 통제 수단은 없는 것과 같다 (AX-210·211)  #### D08-CMP018 EventPanel — `components/EventPanel.js` (파일) · 신설  - 역할: 실시간 변화 **시연** 패널 (6종: CLOSURE · WEATHER · TRAFFIC_SURGE · APPOINTMENT · RISK_SIGNAL · PATIENT_RECALL) - 사용 화면: `/result` - 규칙: 이벤트를 주입하면 **반드시 재판정한다.** 알림만 띄우고 기존 코스를 유지하지 않는다 (D07-BAN008) - 접근성: 비활성은 `disabled` 가 아니라 **`aria-disabled` + 클릭 가드** — `disabled` 는 포커스를 body 로 떨어뜨려 키보드 위치와 모달 포커스 복원을 깨뜨린다 - 차단 조건: 계획 만료·철회 시 패널이 차단된다 (AX-220) - **이것은 시연 도구다.** 휴무·교통을 자동 감지하는 실데이터 공급자는 없다(기상만 실연동)  ### 2.2 규약으로만 존재하는 것 (파일 아님)  아래는 CSS 클래스와 마크업 관례로만 공유된다. 재사용 컴포넌트로 뽑는 것은 미완 작업이다.  | ID | 이름 | 현재 형태 | 비고 | | --- | --- | --- | --- | | D08-CMP001 | SafeHourAppShell | `app/layout.js` 의 `.app-shell` · 화면마다 반복되는 `.top-bar` / `main.page` | header 를 컴포넌트로 뽑지 않아 9화면이 각자 상단 바를 그린다. `skip link` 없음 | | D08-CMP002 | MedicalBoundaryCallout | `.medical-callout` · `role="note"` | `/login` `/today` `/guide` `/plan` `/result` `/place/{id}` `/privacy` 7화면에서 사용. **시작 화면(`/`)만 예외** — 고지를 `.card` 와 `.state-banner` 로 크게 펼친다. `blocking` variant 미구현 | | D08-CMP005 | RoleModeSelector | `/plan` 의 `.toggle-row` 3개 (동행 여부 · 환자 휴식 · 보호자 분리) | 3모드 라디오가 아니라 **체크박스 3개**로 구현됐다. 필수 동행이면 분리 토글이 `disabled` 되고 사유 문구가 함께 뜬다 | | D08-CMP007 | ReturnDeadlineCard | `/today` `/plan` `/result` 의 `.card` 섹션 | **복귀 마감은 복귀시각·복약 시각·다음 진료 중 가장 이른 것**이고 그 사유를 함께 적는다 (`effectiveDeadline`) | | D08-CMP009 | SourceAndUncertaintyPanel | `/place/{id}` 의 개요·운영·추정·접근 `.card` 분할 · `.badge-estimate` `.badge-lang` `.source-note` | 원문과 추정을 다른 카드로 분리. `collapsed/expanded` 상태는 없다 | | D08-CMP011 | SafeEmptyState | `.state-banner` · `<details class="disclosure">` 제외 사유 목록 | 추천 0건은 오류가 아니라 정상 결과. `canRetry` 는 없다 | | D08-CMP013 | ApiErrorRecovery | 화면별 인라인 `.state-banner.state-STANDBY` · `role="alert"` | provider 내부 메시지 비노출은 유지. `retryAfter`·`offline` variant 미구현 | | D08-CMP019 | HospitalIssuedStrip | `.verified-strip`(+`.verified-mark`) · `.badge-hospital` · `.badge-demo` · `.hospital-note` | **신설.** 병원 발행 출처 표시 묶음. §3.2 참고 |  ### 2.3 폐기 (번호 재사용 금지)  | ID | 이름 | 폐기 사유 | | --- | --- | --- | | **D08-CMP003** | FixedAnchorInput | **폐기.** 기준점을 사용자가 고르지 않는다. 병원 계획의 `anchor` 를 읽기 전용으로 표시한다 (`/plan` 기준점 카드) | | **D08-CMP004** | HospitalConditionForm | **폐기 (AX-221).** 수기 입력을 남겨두면 "환자가 한국어 안내문을 해석한다" 는 결함이 그대로 남는다. 조건은 `.badge-hospital` 읽기 전용 배지로만 표시된다 |  > 두 컴포넌트가 사라지면서 `/plan` 의 앵커도 바뀌었다. ADR-0001 이 정의한 > `#location` `#condition` 은 **더 이상 존재하지 않고**, `#role` 만 남는다. → D03 개정 대상  ---  ## 3. 디자인 토큰  ### 3.1 확정 — `app/globals.css` `:root` (코드가 정본)  톤: **클리닉 프리미엄** — 웜 아이보리 바탕, 딥그린 신뢰색, 골드는 얇은 액센트로만.  #### Color (확정)  | 토큰 | 값 | 용도 | | --- | --- | --- | | `--bg` | `#faf8f5` | 바탕 | | `--surface` | `#ffffff` | 카드 | | `--surface-alt` | `#f4f1ec` | 보조 면 | | `--ink` | `#2c2a26` | 본문 | | `--ink-soft` | `#6b6660` | 보조 텍스트 | | `--line` / `--line-strong` | `#e8e2d9` / `#d8d0c3` | 경계 | | `--line-input` | `#948b7c` | **입력 경계 전용** — 흰 배경 위 3.36:1 (WCAG 1.4.11 최소 3:1) | | `--brand` / `--brand-hover` / `--brand-ink` | `#1f4d3d` / `#163a2d` / `#ffffff` | 딥그린 신뢰색 | | `--accent` / `--accent-soft` | `#c9a227` / `#f2e6c4` | 골드. **본문 색으로 쓰지 않는다** — 흰 배경 위 2.4:1. 배지 배경(어두운 글자와 6.6:1)과 2px 규칙선 전용 | | `--state-no-tourism-{bg,ink,line}` | `#fbf0ec` / `#8c3a24` / `#ebd2c6` | NO_TOURISM | | `--state-standby-{bg,ink,line}` | `#fbf5e8` / `#7a5a12` / `#eadcbc` | STANDBY | | `--state-split-{bg,ink,line}` | `#edf3f3` / `#1f4e58` / `#cfe0e1` | SPLIT_NEARBY · **병원 원문 계열로도 사용** | | `--state-together-{bg,ink,line}` | `#eff4f0` / `#1f4d3d` / `#cfe2d6` | TOGETHER |  - `color.focus` 는 별도 토큰이 아니다 — `:focus-visible` 이 `--brand` 3px 아웃라인 + 2px 오프셋, 브랜드 버튼 위에서는 `--ink` 로 교체한다 - 소셜 로그인 버튼은 공급자 브랜드 규정을 따르되 톤을 낮췄다: 카카오 `#fee500` 위 `#191600`(14.3:1), 구글은 흰 배경 + `--line-input` 테두리(3.36:1)  #### Typography (확정)  | 토큰 | 값 | | --- | --- | | `--font-display` | `'Nanum Myeongjo', 'Noto Serif KR', AppleMyungjo, 'Apple SD Gothic Neo', Georgia, 'Times New Roman', serif` — 헤드라인 전용 | | `--font-body` | `-apple-system, BlinkMacSystemFont, 'Apple SD Gothic Neo', 'Pretendard', 'Noto Sans KR', 'Segoe UI', sans-serif` |  - 크기 척도는 **토큰이 아니라 요소 선택자**에 박혀 있다: `body` 16px/1.6 · `h1` 28px · `h2` 20px · `h3` 17px · `.btn` 17px · `.badge` 11px · `.hint`·`.source-note` 13px - **본문 최소 16px 규칙**: 안전 판단에 쓰이는 수치(`.meta`)와 변화 설명(`.delta-row`)은 14px 로 줄이지 않고 16px 를 유지한다 - 웹폰트를 로드하지 않는다. 세리프가 없는 기기에서는 조용히 산세리프로 떨어진다 - 한국어·영문 동시 가독성 폰트 **선정은 미완** — 현재는 시스템 폰트 스택이다  #### Shape (확정)  | 토큰 | 값 | | --- | --- | | `--radius` | `10px` | | `--radius-sm` | `8px` | | `--shadow-card` | `0 1px 2px rgba(44,42,38,.04), 0 8px 24px -16px rgba(44,42,38,.18)` |  ### 3.2 세 번째 출처 — 병원 원문 (신설)  출처가 2영역에서 3영역이 됐다. 시각 규칙은 이렇게 확정됐다.  | 출처 | 시각 처리 | | --- | --- | | **병원 원문** (채널 B) | 틸 계열(`--state-split-*`). `.hospital-note`(좌측 3px 굵은 선 · `--surface-alt` 배경) 안에 `.badge-hospital` "병원에서 제공한 안내" 배지 · 발행·수정 시각 |
| **SafeHour 판정·추정** | 골드 `.badge-estimate` "추정" 배지. 판정 문구는 `.state-banner` | | **관광정보 원문** | `.badge-lang` 언어 배지 · `.source-note` 출처·저작권 문구 |  - `.verified-strip` — 화면 상단 고정. 발행 병원명 + 유효기간 + **데모면 `.badge-demo`** - **`병원 연동 데모` 배지는 숨기지 않는다.** 실제 연동으로 오인되면 공모전 요강의 허위 제출 조항에 걸린다 (정의 §9-3)  ### 3.3 미토큰 — 아직 값이 흩어져 있다  | 항목 | 현재 | | --- | --- | | **Spacing** | `space.1–8` 토큰이 **없다.** 여백은 CSS 규칙과 JSX 인라인 `style` 에 리터럴 px 로 박혀 있다 (9화면 · 9컴포넌트에 인라인 `style` **92곳**) | | **Layout** | `layout.content.max` 토큰 없음. `.app-shell` `.sticky-return` `.sheet` 가 각각 `max-width: 480px` 를 반복한다 | | **Touch** | `size.touch.min` 토큰 없음. 44px/48px/52px/54px 를 컴포넌트마다 직접 지정하고, **E2E 테스트가 44px 미만을 잡는다** | | **Motion** | `motion.duration.*` 토큰 없음. 애니메이션은 스피너 회전 하나뿐이며 `prefers-reduced-motion: no-preference` 안에서만 돈다. 기본 형태(원형 테두리)는 모션 없이도 항상 보인다 |
| **Dark mode** | 없음. 라이트 단일 팔레트 |  > **여백 토큰화와 인라인 스타일 정리가 이 문서의 가장 큰 미완 작업이다.** 색은 코드가 > 정본이 됐지만 여백은 아직 정본이 없다.  ---  ## 4. 반응형 계약  - 단일 컬럼. `.app-shell` `max-width: 480px` 중앙 정렬 — **모바일 전용 레이아웃이며
  데스크톱 2열 분기는 구현하지 않았다** (이전 문서의 768/1024 2열 계약은 미적용) - 하단 여백 `padding-bottom: 128px` 로 sticky 복귀 CTA 가 본문을 가리지 않게 한다 - 가로 스크롤 금지 — **320 / 360 / 430 / 768 / 1280px 5개 폭에서 E2E 로 단언**한다 (320px 는 WCAG 1.4.10 reflow 기준 폭) - 텍스트 200% 확대에서도 가로 스크롤이 생기지 않는다 (WCAG 1.4.4, E2E 검증) - 상단 바 제목은 좁은 화면에서 줄바꿈 대신 `text-overflow: ellipsis` 로 줄인다. 접근 가능한 이름은 DOM 에 그대로 남는다 - 시트는 `max-height: 85dvh` + 내부 스크롤, `env(safe-area-inset-bottom)` 적용 - **미구현**: 결과 카드 이미지. 장소 상세에만 이미지가 있고 추천 카드에는 없다. 따라서 "이미지 aspect ratio 고정·깨진 이미지 fallback" 은 상세 화면 한정 계약이다  ---  ## 5. 접근성 계약과 실측  ### 5.1 두 언어 병기 규칙 (ADR-0003 §5)  > **안전 문구는 사용자가 선택한 언어와 무관하게 두 언어로 보인다.**  | 대상 | 병기 방식 | | --- | --- | | 상태 배너 (CMP006) | 현재 언어 문구 `h2` · 반대 언어 문구 `p lang="{other}"` 14px | | 즉시 복귀 시트 (CMP012) | 제목·본문을 반대 언어로 한 줄 함께 출력 | | 그 밖의 화면 문구 | 병기하지 않는다. 선택한 언어 하나만 |  이유: 언어를 바꾸지 못한 사용자, 그리고 **환자와 보호자의 언어가 다른 경우**를 위한 안전장치다.  규칙 두 가지:  1. **병기 문구의 대비를 낮추지 않는다.** `opacity` 감산 금지 — 상태 배너 배경 위에서 4.5:1 을 넘지 못한다 (코드 주석에 근거 명시) 2. **반대 언어 문구에는 반드시 `lang` 속성을 붙인다.** 스크린리더가 잘못된 음성으로 읽지 않게  안전 문구의 정본은 도메인 계층(`src/domain/states.js` 의 `STATE_MESSAGE` · `REASON_TEXT`)이며 ko/en 을 모두 갖는다. 화면 사전이 안전 문구를 다시 정의하지 않는다.  ### 5.2 계약  - WCAG 2.2 AA 목표 (자동 검사 게이트는 `wcag2a` `wcag2aa` `wcag21a` `wcag21aa` 태그) - 상태·오류·변경을 색상만으로 전달 금지 — 탭은 하단 바 + 굵기, 라디오 선택은 테두리 색 + 두께 + 배경, 상태 배너는 `.state-label` 텍스트 라벨 병행 - `:focus-visible` 을 UA 기본에 맡기지 않고 3px 아웃라인으로 명시. 카드형 라디오·토글은 내부 22~28px 입력이 아니라 **카드 전체**에 `:focus-within` 링을 표시 - **`disabled` 대신 `aria-disabled` + 클릭 가드** — `disabled` 는 포커스를 body 로 떨어뜨려 키보드 위치와 모달 포커스 복원을 깨뜨린다. 시각 표시는 opacity 감산이 아니라 색 교체 (대비 유지) - 모달: `role="dialog"` `aria-modal` `aria-labelledby`, 포커스 트랩, 닫으면 트리거로 복원. Escape·뒤로가기·확인 버튼이 **모두 같은 닫기 경로**를 탄다 (히스토리 잉여 항목 방지) - 재판정 갱신 고지는 `aria-live="polite"`. 상태 배너 본문은 `role="status"` 로 덮어쓰지 않는다 - 원문에는 원문 언어 `lang` 표기, `document.documentElement.lang` 실제 갱신 - `.visually-hidden` 으로 링크에 장소명을 덧붙여 "상세 보기" 링크가 문맥 없이 반복되지 않게 한다  ### 5.3 실측 (2026-08-22 · E2E)  | 항목 | 결과 | | --- | --- | | axe WCAG A/AA 자동 검사 | **9화면 위반 0** — `/` `/login` `/link` `/today` `/guide` `/plan`(연결·미연결) `/result`(추천·미추천·델타 시트·복귀 시트) `/place/{id}` `/privacy` | | 키보드 전용 조작 | **7건 통과** — ① 마우스 없이 판정 제출 ② 코스 탭 방향키 전환 ③ Escape 로 시트 닫기 · 포커스 복원 ④ 뒤로가기가 시트만 닫음 ⑤ 확인 버튼이 히스토리에 잉여 항목을 남기지 않음 ⑥ 겹친 시트 하나씩 닫힘 ⑦ 모달 포커스 트랩 | | 반응형 | 320/360/430/768/1280px 가로 스크롤 0 | | 확대 | 텍스트 200% 가로 스크롤 0 | | 터치 대상 | 결과 화면의 보이는 모든 `button`·`a` 높이 44px 이상 | | 언어 | 전 화면 ko/en, `lang` 갱신, 안전 문구 병기 |  > **자동 검사는 접근성의 바닥이지 천장이 아니다.** > **스크린리더 검증은 미실행**이고 실기기 확인도 남아 있다 (`docs/DEVICE_TEST_CHECKLIST.md` 5절). > D09-AC019 는 이 두 가지 때문에 아직 닫히지 않았다. > (`docs/DEVELOPMENT_READINESS.md` 의 AC019 행은 "키보드 4건" 으로 남아 있어 실제 7건과 > 어긋난다 — 해당 문서 갱신 필요)  ---  ## 6. Figma·코드 매핑  | 항목 | 상태 | | --- | --- | | Figma file URL | **[확인필요] — 없음.** 코드가 디자인보다 앞서 있다 | | 화면 node | [확인필요] | | 컴포넌트 set | 파일 9 · 규약 8 · 폐기 2 (§2) | | Code framework | Next.js App Router (JS, TypeScript 미사용) | | 토큰 정본 | **`app/globals.css` 의 `:root`** — 색·글꼴·모서리·그림자 | | Storybook·컴포넌트 문서 | **없음.** E2E 스펙이 사실상의 컴포넌트 계약서 역할을 한다 | | Version | code v2 (병원 지침 연동), design 원본 없음 | | Owner | 디자인 리드 [지정필요] · 프론트엔드 리드 [지정필요] |  ### 화면 ↔ 라우트  | 화면 | 라우트 | D03 ID | | --- | --- | --- | | 시작·안전 안내 | `/` | D03-SCR001 | | 로그인 | `/login` | **신설 — D03 개정에서 부여** (ADR-0004) | | 병원 연결 | `/link` | **신설 — D03 개정에서 부여** | | 오늘의 회복 (홈) | `/today` | **신설 — D03 개정에서 부여** | | 병원 회복 안내 | `/guide` | **신설 — D03 개정에서 부여** | | 안전 외출 (계획 확인) | `/plan` (`#role` 앵커만 남음) | D03-SCR002–004 통합 → **SCR002·003 은 폐기 대상** | | 판정 결과 | `/result` | D03-SCR005 | | 변화 전후 비교 | `/result` 내 모달 | D03-SCR007 | | 즉시 복귀 | `/result` 내 모달 | D03-SCR008 | | 장소 상세 | `/place/{candidateId}` | D03-SCR006 | | 개인정보 고지 | `/privacy` | **신설 — D03 개정에서 부여** |  > 새 화면 5종의 SCR ID 부여는 **D03 의 권한**이다. 이 문서에서 임의로 번호를 만들지 않는다.  ### 디자인 QA 체크  - 320px · 360px · 430px · 768px · 1280px - ko · en 긴 문구 - 200% 확대 - keyboard-only - **screen reader (미실행)** - slow 3G · image error - `NO_TOURISM` · `STANDBY` · `SPLIT_NEARBY` · `TOGETHER` - 변화 replacement · return_now - **연결 게이트 5종**: 지침 없음 · 만료 · 철회 · 미확인 중요 변경 · 복약 창 - **데모 배지 노출** (`/link` `/plan`)  ---  ## 7. 허용·금지  ### 허용  - 안전 상태와 이유를 반복 표시 - unknown · stale · fallback · 추정 · 데모 배지 - 병원 원문 / SafeHour 판정 / 관광정보를 각각 다른 블록으로 분리 - 추천 없는 결과에 명확한 다음 행동 (액션은 사라지지 않고 성격이 바뀐다 — 정의 §5.2)  ### 금지  - 빨간색만으로 위험 표시 - NO_TOURISM 에 실패 일러스트만 사용 — **정상 결과 배너다** - 확인되지 않은 의료·영업·접근성 정보를 check 아이콘으로 표현 - 병원·관광 데이터 제공기관 로고를 보증 마크처럼 사용 - 환자·보호자 모드를 색상만으로 구분 - 현재 GPS 아이콘·버튼으로 오해 유발 — **GPS 를 수집하지 않는다** - 골드(`--accent`)를 본문 글자색으로 사용 (2.4:1) - 상태 배너 병기 문구에 `opacity` 감산 - 안전 CTA 를 `disabled` 로 만들어 포커스를 잃게 하기 - **병원 원문(채널 B)을 SafeHour 문구와 같은 블록에 섞기** — 재해석으로 읽힌다 - **`병원 연동 데모` 배지를 숨기거나 축약하기** - 평점·인기·제휴·광고를 추천 카드에 표시  ---  ## 8. 미해결 항목  ### D08-UNRESOLVED-001 디자인 시스템 — **부분 해소**  - 이전 질문: 브랜드·폰트·토큰·기존 컴포넌트 시스템은 무엇인가? - **해소된 부분**: 색·글꼴 스택·모서리·그림자가 코드로 확정됐다 (클리닉 프리미엄 — 웜 아이보리 + 딥그린 + 골드). 대비 근거가 주석으로 남아 있다. - **남은 부분**: 여백 토큰 없음. 인라인 `style` 92곳. `max-width: 480px` 반복. 한국어·영문 동시 가독성 폰트 선정 없음(시스템 스택). 다크 모드 없음. - Blocking Level: design_blocker (해소 진행 중)  ### D08-UNRESOLVED-002 Figma 정본 — **미해소**  - 질문: Figma file·node·version·owner 를 언제 생성·승인할 것인가? - 상황이 뒤집혔다 — **코드가 앞서 있으므로 Figma 는 "새로 그리는 원본" 이 아니라
  "코드에서 역추출한 정본" 이 된다.** 어느 쪽을 정본으로 삼을지가 먼저 정해져야 한다. - Blocking Level: design_blocker  ### D08-UNRESOLVED-003 프론트엔드 구현 기반 — **해소**  - 결정: Next.js App Router · JS · CSS 변수 기반 단일 스타일시트. - 다만 **Storybook·컴포넌트 문서는 없다.** 컴포넌트 계약을 읽으려면 E2E 스펙을 봐야 한다.  ### D08-UNRESOLVED-004 컴포넌트 추출 — **신규**  - 질문: 규약으로만 존재하는 8개(§2.2)를 파일 컴포넌트로 뽑을 것인가? - 특히 `.top-bar` 는 9화면이 각자 그리고 있어 변경 비용이 9배다. `skip link` 도 없다. - Blocking Level: development_blocker(낮음)  ### D08-UNRESOLVED-005 보호자 화면 — **신규**  - 게이트웨이의 **보호자 축약 payload 는 구현돼 있으나, 부르는 곳도 볼 화면도 없다.** `/share` 라우트는 정의에만 있고 코드에 없다. - 따라서 이 문서에 보호자 전용 컴포넌트는 **한 개도 없다.** - Blocking Level: design_blocker  ### D08-UNRESOLVED-006 지도·경로 표시 — **신규**  - 즉시 복귀 시트에 **지도가 없다.** 좌표 복사 + 병원 연락 안내가 전부다. - 미구현이 아니라 **의도적으로 멈춘 것**이다 — 위치정보 사전검토 회신 대기(문의 2540), 공급자·폴백·비용 미결, 경로 API 별도 승인 필요. - 회신이 와도 바로 붙지 않는다. 지도가 들어오면 CMP012 의 시각 계약을 다시 연다. - Blocking Level: external_blocker  ### D08-UNRESOLVED-007 표시되지만 판정에 쓰이지 않는 조건 — **신규**  - `/plan` 은 병원 조건을 `.badge-hospital` 배지로 나열하는데, 그중 **열 노출 회피
  (`avoidHeat`)·수중 활동 금지(`noWater`)는 화면에만 뜨고 판정에는 쓰이지 않는다.** `planToCondition` 이 두 값을 엔진 조건으로 옮기지 않는다. - **금식 종료 시각(`fastingUntil`)은 boolean 으로 접혀** 시각 정보가 판정에서 사라진다. - 배지가 "이 조건이 반영됐다" 로 읽히므로 **시각 표현이 사실보다 앞서 있다.** 배지에 반영 여부를 구분하는 표시가 필요한지 결정해야 한다. - Blocking Level: safety_review  ### D08-UNRESOLVED-008 QR 스캔 UI — **신규**  - `/link` 문구는 "QR 또는 코드" 를 안내하지만 **카메라 스캔은 없다.** 실제 입력 수단은 텍스트 코드 하나다. - 문구와 구현이 어긋난 상태이므로 문구를 고치거나 스캔을 넣어야 한다. - Blocking Level: content_blocker  ### D08-UNRESOLVED-009 영문 문구 검수 — **신규**  - 전 화면 영문이 생겼으나 **번역 검수는 미완료**이고 **확인된 오역이 있다** (`docs/SIGNOFF_CHECKLIST.md` 5절 · `docs/REVIEW_EN_SAFETY_COPY.md`). - 안전 문구는 두 언어로 항상 병기되므로, 오역은 한국어 화면에서도 그대로 노출된다. - 담당자 [지정필요] - Blocking Level: content_blocker  ### D08-UNRESOLVED-010 스크린리더 검증 — **신규**  - axe 자동 검사와 키보드 7건은 통과했으나 **스크린리더·실기기 확인은 미실행**이다. - 특히 두 언어 병기 블록이 실제로 올바른 음성으로 읽히는지는 자동 검사로 확인되지 않는다. - 절차: `docs/DEVICE_TEST_CHECKLIST.md` 5절 - Blocking Level: quality_gate  ### D08-UNRESOLVED-011 병기 규칙이 적용되지 않는 상태 표시 — **신규**  - `.state-banner` 는 컴포넌트(CMP006)와 인라인 마크업 **두 갈래**로 쓰인다. 인라인 쪽은 두 언어 병기가 적용되지 않는다. - 가장 문제가 되는 자리: `/place/{id}` 의 차단 화면이 **`state-NO_TOURISM` 배너를
  단일 언어로** 그린다. 안전 상태 표시인데 병기 규칙 밖에 있다. - 같은 성질: `/today` 미연결(`state-STANDBY`), `/link`·`/login`·`/plan` 의 오류 배너. 오류 배너는 4상태가 아니므로 병기 대상인지 별도 판단이 필요하다. - 결정 필요: 병기 규칙의 적용 범위가 **"4상태 전부" 인가 "판정 결과 화면만" 인가.** - Blocking Level: safety_review  ---  **사람 검토 체크:** 컴포넌트 실체 구분(파일/규약/폐기), 폐기 2건의 타당성, 확정 토큰 값,
세 번째 출처(병원 원문) 시각 규칙, 두 언어 병기 규칙, 접근성 실측 수치, 새 화면 5종의
SCR ID 부여(D03), UNRESOLVED-007 의 안전 영향
